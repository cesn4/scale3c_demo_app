import 'package:flutter/material.dart';

class ThemeBorderRadius {
  static BorderRadius primary = BorderRadius.all(Radius.circular(20.0));
  static BorderRadius secondary = BorderRadius.all(Radius.circular(30.0));
}
